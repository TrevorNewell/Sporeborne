# Assignment #5 -- Goal-Oriented Coding Agent

Generated by `Claude/goal_agent.py`. Each run appends what it built and why below; fill in "What I changed before accepting it" yourself after reading the generated code.

## Built: G2-1
- **Why this feature**: ranked #1 by `priority_score()` among 30 GDD-derived candidates.
- **Files written**: AI/SporeborneAIController.h, AI/SporeborneAIController.cpp, Characters/SporeborneEnemyCharacter.h, Characters/SporeborneEnemyCharacter.cpp
- **Action required**: add to Build.cs -> AIModule, NavigationSystem
- **Agent's reasoning**: The feature is a minimal but genuinely fun enemy AI that satisfies G2 ('one room is genuinely fun to clear'). It consists of two tightly-coupled classes: ASporeborneAIController (a ticker-based state machine with Idle/Chase/Attack states driven by distance) and ASporeborneEnemyCharacter (health, melee attack via UGameplayStatics::ApplyDamage, and a ragdoll death). The controller auto-possesses any placed or spawned enemy. Parameters (detection range, attack range, cooldown, damage, health) are all EditDefaultsOnly so a designer can tune them in the Blueprint subclass without touching code. Two engine modules beyond the base four are needed: AIModule (for AAIController and MoveToActor) and NavigationSystem (for the nav-mesh path-finding that MoveToActor relies on).

## What I changed before accepting it
_Draft below from the compile session -- edit freely, this is meant to be your own words._

- Added `AIModule` and `NavigationSystem` to `Sporeborne_Rogue.Build.cs` -- the agent
  correctly flagged these as required (for `AAIController`/`MoveToActor`) but didn't
  add them itself, since a build-dependency change should be a reviewed human step.
- Fixed two real compile errors in the generated `.cpp` files: (1) the includes used
  the short form (`"AI/Foo.h"`) but this module's actual include search path is
  `Source/`, not `Source/Sporeborne_Rogue/` -- confirmed against the compiler's own
  response file -- so cross-subfolder includes need the module name prefixed
  (`"Sporeborne_Rogue/AI/Foo.h"`); (2) `GetCapsuleComponent()->SetCollisionEnabled(...)`
  needs `Components/CapsuleComponent.h` included directly, since `Character.h` only
  forward-declares `UCapsuleComponent`.
- Regenerated Visual Studio project files (`UnrealBuildTool -projectfiles`) so the new
  `AI/` and `Characters/` source folders show up in the IDE.
- Confirmed `Sporeborne_RogueEditor` builds clean (`Build.bat`, Development, Win64).
- Not yet done: placing an `ASporeborneEnemyCharacter` in a level with a
  NavMeshBoundsVolume to see it actually chase/attack at runtime.
